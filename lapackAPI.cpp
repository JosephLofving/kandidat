#include <iostream>
#include <iomanip>
#include <vector>
#include <stdlib.h>

//denna funktion behövs inte...
void matprint(int mat[][]) {
	col_maxes =
		for (col : mat) {
			for (x : col) {
				int col_maxes =
			}
		}
}